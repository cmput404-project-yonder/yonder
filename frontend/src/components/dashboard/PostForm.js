import React, { Component } from "react";
import ThreadDisplay from "../dashboard/ThreadDisplay";

class PostForm extends Component {
    render() {
        return (
            <ThreadDisplay/>
        )
    }
}

export default PostForm